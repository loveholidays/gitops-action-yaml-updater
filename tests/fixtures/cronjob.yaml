apiVersion: batch/v1
kind: CronJob
metadata:
  name: test-cronjob
spec:
  schedule: "0 */6 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: test-cronjob
              image: eu.gcr.io/test/cronjob:v1.0.0
          restartPolicy: OnFailure
